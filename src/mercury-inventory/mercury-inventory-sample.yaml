inventory:
  bind_address: tcp://0.0.0.0:9000

  db:
    name: mercury
    collection: inventory
    servers:
      - localhost:27017
    replica_name: null


logging:
  log_level: INFO
  debug_asyncio: False