db:
  rpc_mongo_db: mercury
  rpc_mongo_collection: rpc
  rpc_mongo_servers:
    - localhost:27017
  rpc_mongo_replica_set: None

inventory:
  inventory_router: tcp://localhost:9000

fontend:
  rpc_serivce_bind_url: tcp://0.0.0.0:9001

backend:
  registration_service_url: tcp://0.0.0.0:9002

workers:
  max_request_threads: 1000
  max_queue_runners: 1000
  max_ping_threads: 10000
